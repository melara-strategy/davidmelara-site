# Documentación interna del proyecto — no se publica ni se versiona en este repo público.
# El contenido permanece en el disco local; su reorganización (repo privado u otra
# estrategia de almacenamiento) se hará por separado.
docs/

# Estado efimero del servidor de previsualizacion de Impeccable (pid, puerto, token de sesion).
.impeccable/live/

# Especificacion de la fase anterior (evidencia primero). Superada por PRODUCT.md y DESIGN.md.
IMPLEMENTACION_WEB_V1.md

# Estado canónico del proyecto: documento de trabajo interno, no se versiona ni se sirve.
# _redirects lo bloquea además en producción, como salvaguarda si se comiteara por error.
ESTADO_CANONICO_PROYECTO_V1.md
